import React from 'react';
import Card from '@material-ui/core/Card';

const CategoryCard = (props) => {
  const styles = {
    list: {
      overflow: 'auto'
    },
    listItem: {
      maxHeigt: 200,
      maxWidth: 600
    }
  }

  return (
    <Card
      style={styles.list}>
      
    </Card>
  )
}
